# Shell uploader to hpaste.org 

## Installation 

Since it is a shell script which just uses the curl utility so just download it and start
using, or copy it to bin directory for global usage. 
For more info use:
    hpaste -h 

